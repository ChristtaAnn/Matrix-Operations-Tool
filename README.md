# Matrix-Operations-Tool
Create a "Matrix Operations Tool" using Python and the NumPy library. The application should allow users to input matrices and perform operations like addition, subtraction, multiplication, transpose, and determinant calculation. Include an interactive interface to display results in a structured format.
# Matrix Operations Tool (Python + NumPy)

## 📌 Overview
The **Matrix Operations Tool** is a Python-based, menu-driven application that allows users to perform common matrix operations using the **NumPy** library.  
The program provides an interactive interface to input matrices and execute operations such as addition, subtraction, multiplication, transpose, and determinant calculation.

---

## ✨ Features
- User-defined matrix input (any size)
- Matrix addition
- Matrix subtraction (A − B or B − A)
- Matrix multiplication (A × B or B × A)
- Matrix transpose
- Determinant calculation
- Input validation for matrix dimensions
- Interactive, menu-driven interface
- Handles floating-point precision issues

---

## 🛠️ Technologies Used
- **Python 3**
- **NumPy**

---


---

## 🚀 How to Run the Program

### 1️⃣ Install NumPy (if not installed)
```bash
pip install numpy
